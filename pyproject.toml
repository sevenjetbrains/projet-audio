[project]
name = "audiocut-studio"
version = "0.1.0"
description = "Extraction, nettoyage et fusion de séquences audio à partir de vidéos."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
